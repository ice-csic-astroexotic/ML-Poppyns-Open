# Dynamical simulation example

This is an example of a dynamical simulation run with the `simulate_population_dyn.py` script and using the default parameters in the configuration file.

To run this example, we use the following command:
```commandline
python mlpoppyns/simulator/simulate_population_dyn.py --save_dir data/example_simulation_dyn
```